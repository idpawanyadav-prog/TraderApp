# TraderApp Python dependencies
# Python 3.6.x compatible
# Vendored packages (in libs/) are bundled — install system packages only if needed.

# --- Web framework (vendored in libs/) ---
Flask==2.0.3
Flask-SocketIO==5.1.1
Werkzeug==2.0.3
Jinja2==3.0.3
itsdangerous==2.0.1
click==8.0.4
MarkupSafe==2.0.1

# --- SocketIO / async transport (vendored in libs/) ---
python-socketio==5.4.0
python-engineio==4.2.1
bidict==0.21.4
simple-websocket==0.5.1
h11==0.13.0
wsproto==1.0.0

# --- HTTP client (vendored in libs/) ---
requests==2.27.1
urllib3==1.26.20
certifi==2025.4.26
charset-normalizer==2.0.12
idna==3.10

# --- Auth ---
pyotp==2.7.0

# --- Data / Technical Analysis (system packages) ---
numpy==1.19.5
pandas==1.1.5
ta==0.11.0
python-dateutil==2.8.2
pytz==2022.7.1
six==1.16.0

# --- Python 3.6 backports (vendored in libs/) ---
dataclasses==0.8
importlib-metadata==4.8.3
typing-extensions==4.1.1
zipp==3.6.0

# --- Windows utils (system) ---
colorama==0.4.5

