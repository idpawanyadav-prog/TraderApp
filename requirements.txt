# TraderApp Python dependencies
# Requires Python 3.10 or newer (including 3.14).

# --- Web framework ---
Flask>=3.1.0,<4
Flask-SocketIO>=5.6.0,<6
Werkzeug>=3.1.0,<4
Jinja2>=3.1.2,<4
itsdangerous>=2.2.0,<3
click>=8.1.3,<9
MarkupSafe>=2.1.1,<4
blinker>=1.9.0,<2

# --- SocketIO / async transport ---
python-socketio>=5.12.0,<6
python-engineio>=4.11.0,<5
bidict>=0.23.0,<1
simple-websocket>=1.1.0,<2
h11>=0.14.0,<1
wsproto>=1.2.0,<2

# --- HTTP client ---
requests>=2.32.0,<3
urllib3>=2.2.0,<3
certifi>=2024.0.0
charset-normalizer>=3.0.0,<4
idna>=3.7,<4

# --- WebSocket client (for 5Paisa Option Greeks) ---
websocket-client>=1.8.0,<2

# --- Auth ---
pyotp>=2.9.0,<3

# --- Data / analysis ---
# NumPy 2.3.3+ is required on Python 3.14
numpy>=2.2.6,<3; python_version < "3.14"
numpy>=2.3.3,<3; python_version >= "3.14"

# --- Console colors (macOS/Linux/Windows) ---
colorama>=0.4.6,<1

# --- Excel (xlwings works on Windows and macOS with Excel for Mac) ---
xlwings>=0.29.0,<1
# pywin32 (Excel COM) is Windows-only
pywin32>=306; sys_platform == "win32"
